import React from 'react';
import './TopBar.scss';

const TopBar = () => (
  <div className="top-bar">
    <div className="top-bar-content">Some content in here</div>
    <div className="top-bar-content">Some content in here</div>
  </div>
);

export default TopBar;